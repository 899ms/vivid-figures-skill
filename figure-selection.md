# 从数据与目的选择模板

用于首次选择数值图模板；这是原工作流中“浏览配方、选择候选”的具体做法，完成后直接继续原来的完整配方加载与绘制，不重复选图。技术图等仍按原入口路由。以下路径相对于 `<SKILL>`，不是 `original/resources/`。

## 1. 理解数据和表达目的

先结合用户要求、论文上下文和实际数据，明确这张图想让读者看懂什么：主要比较对象、关注的关系或变化，以及需要保留的分布、波动或结构。区分用户明确表达的结论与仍需数据验证的问题。没有给目的时，从上下文提出合理的表达方向；只有缺失信息会实质改变含义时才询问，不增加固定问卷或确认步骤。

复用已知的数据摘要；不清楚时再检查必要字段。可用宿主的数据工具，或运行事实摘要脚本：

```bash
python "<SKILL>/scripts/profile_figure_data.py" "<WORKSPACE>/data.csv"
python "<SKILL>/scripts/profile_figure_data.py" "<WORKSPACE>/data.xlsx" --sheet Sheet1 --columns method score seed --group method --output "<WORKSPACE>/data-profile.json"
```

脚本输出行数、类型、缺失、有限数值范围、分位数、类别及组内有效样本数。ID、日期等仅是待解释的线索。模型还需结合字段含义判断观测单位、变量角色、时间顺序、是否配对，以及 SD/SE/CI 的来源；不能仅凭列名、数值类型或重复值推断这些语义。不要为套图擅自补造样本、误差或显著性。摘要有截断时按需读取其余相关字段。

## 2. 全库检索，再看实际图像

读取 [精简目录](catalog/selection-index.jsonl)，按表达目的、数据要求、组合结构和视觉特点一起寻找候选，兼顾直接表达和能增加信息层次的方案。可以先用关键词搜索，但不能只匹配图名；词语不匹配或候选单一时，浏览全目录及相关候选。所有模板作为一个库检索，标签可重叠；按用途、数据要求和图形结构选择。配方 ID 仅用于定位，不用其前缀限定候选。

使用宿主看图工具查看候选的 `preview`，需要时放大 [预览目录](catalog/previews/) 中的完整图片，并阅读对应 `card_path`。比较数据是否能支撑图的结构、视觉特点是否有助于表达、实际篇幅是否合适；不要只读文件名或卡片文字就声称看过图。通常比较几个有区别的候选即可，不设置固定数量或评分。预览配色不决定模板是否适合，选色仍按现有配色指导执行。

用户明确指定模板时直接定位卡片、实图及源码；修复或换色时沿用已有模板，不重做这条流程。看图能力暂不可用时如实说明，根据卡片和代码选择，不假装完成视觉比较。

## 3. 加载完整配方，进入原绘图流程

在现有图表计划或任务记录里保留所选 ID、预览路径和简短理由即可，无需新增表单或审批。卡片帮助找图，不替代原绘图指导与完整源码。

143 个配方（含新增截图恢复模板）均用当前资源提取器将完整主示例写入工作脚本：

```bash
python "<SKILL>/original/resources/assets/shared-scripts/get_recipe.py" --id advanced.pair_plot --script figures/gen_fig_pair_plot.py
```

完整组合模板 `template.sem_violin_pearson` 使用 [专用说明](templates/sem-violin-pearson/TEMPLATE.md)。选定后按 [源码底稿与保真要点](template-fidelity.md)，复制所选完整源码，在原结构内适配数据，动笔前读取所选卡片的保留重点与允许适配范围；随后继续原工作流的绘制、看图和修复。

`template.shap_dependence` 与 `template.shap_contribution` 使用 [SHAP完整组合说明](templates/shap-composites/TEMPLATE.md) 和共享脚本的对应模式，不通过配方提取器调用。两套分别用于“重要性与特征响应”和“变量及类别贡献”，均需要对应的特征值与SHAP值。

目录中的 `sources/` 是解释预览的历史证据，实际绘制加载当前配方。更新配方或图库时核对来源与预览是否一致，不在每次绘图前新增全库验证。当前卡片覆盖每个配方的主要示例；聚类热力图的另一个完整变体保存在原章节中，尚无独立预览，可用 --variant two_way 提取并接入真实输入。
