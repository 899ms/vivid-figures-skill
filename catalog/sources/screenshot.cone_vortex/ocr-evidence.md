# 代码截图的辅助OCR转录（未经逐字校订，不可直接运行）

以原始截图为准；可运行恢复版见 restored.py。

## 截图 028

```text
BEB: =I py @®
import plotly.graph_objects as go
import pandas as pd
df = pd.read_csv("vortex.csv")
fig = go.Figure(
data=go.Cone(
x=df["x"],
yedf["y"],
z=df["2"],
u=df["u"],
vedf["v"],
w=df["w"],
colorscale=["#ffffff", "#e9dlab", '#c3476a"],
sizemode="absolute",
sizeref=60,
)
)
fig.update_layout(
margin=dict(1=8, r=0, t=O, b=0),
scene=dict(
xaxisedict(title="X", backgroundcolor="#f7effe") ,
yaxis=dict(title="¥", backgroundcolor="#f7effo") ,
zaxis=dict(title="Z", backgroundcolor="#f7effe") ,
aspectratio=dict(x=0.7, y=1, z=@.5),
ys
)
fig.show()
1

```

