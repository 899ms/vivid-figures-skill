--- original.py
+++ render.py
@@ -70,7 +70,7 @@
     rank = i + 1
     if rank <= 3:
         badge = plt.Circle((badge_x, y_pos[i]), 0.3,
-                            color=_lighten(c, 0.15), zorder=6,
+                            color=_lighten(mc.to_hex(c), 0.15), zorder=6,
                             transform=ax.transData)
         ax.add_patch(badge)
         ax.text(badge_x, y_pos[i], str(rank),
@@ -78,7 +78,7 @@
                 ha='center', va='center', zorder=7)
     else:
         ax.text(badge_x, y_pos[i], str(rank),
-                fontsize=7.5, color=_lighten(c, 0.2),
+                fontsize=7.5, color=_lighten(mc.to_hex(c), 0.2),
                 ha='center', va='center', fontweight='bold')
 
 # 第一名背景高亮条
