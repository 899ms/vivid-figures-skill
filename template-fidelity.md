# 从完整源码适配

用于选定模板后的绘制、修图和换色。默认保留所选实现，在该实际源码文件上修改；不凭印象重新写一个同类型图。卡片的保留重点描述该示例，不是全库统一数值阈值。

## 1. 准备工作脚本

初始化后，在任务工作区执行：

```text
python _utils/get_recipe.py --id empirical.prediction_ci --script figures/gen_fig_forecast.py
```

工具核对完整配方及注册代码块，写入可执行Python源码，保存 `.vivid/template-sources/` 原始底稿与来源记录，并返回路径、状态及所选卡片的保留和适配要点。读取生成的工作脚本，以及所选卡片的保留要点和预览；无需再次把相同底稿全文打印一遍。

有现成计划时可批量准备（计划中用 `recipe:empirical.prediction_ci` 等引用）：

```text
python _utils/get_recipe.py --plan FIGURE_PLAN.json PAPER_PLAN.md --scripts-dir figures
```

`--list-variants --id advanced.cluster_heatmap` 查看注册变体，`--variant two_way` 选择双向聚类；该变体需接入真实 `data`、`labels_row`、`labels_col`，不是独立随机数据演示。不要将章节里所有代码块拼接。旧的 `--id` / `--output` 保留完整Markdown提取功能，不能用 `--output x.py` 代替源码接口。

恢复时再次执行会保留已登记的适配；同名未登记文件、版本不一致或底稿缺失会报错，先核对文件，必要时用新路径准备当前版本。来源记录只能证明初始源码及保存底稿，没有防篡改权限保证。

完整组合先用专用工具保存完整脚本和来源，再按对应 TEMPLATE.md 的原CLI执行，只把命令中的脚本路径换为工作文件：

```text
python "<SKILL>/scripts/prepare_complete_template.py" --id template.shap_dependence --script figures/plot_shap.py --workspace .
```

三套组合保持各自输入和模式；未改动的原CLI同样可直接使用，其来源是包内完整脚本。适配时使用已登记工作副本，不拆成普通配方重拼。

## 2. 在刚生成的文件上修改

先用文件编辑/局部补丁替换刚生成文件中的演示数据段，再修改确有需要的布局。不要把“工具复制成功”当作随后整段重写的许可；复制只是起点，交付的绘图主体也须沿用原实现。

用文件编辑/补丁替换演示的数据段，例如将原 `actual = ...` 的演示数组替换为从CSV读取、按真实时间排序的 `actual`，然后更新横坐标、区间和标签。保留后面的折线、区间层、误差面板、图例等实现，按真实统计含义调整变量绑定。演示数据段全部移除，不能保留模拟指标冒充实际结果。

允许共享清洗和统计函数；主体绘图代码与来源对应。组数或站点增加时复用原绘图结构，合理扩展画布或面板，不能为了保持演示形状漏数据。不同单位不能无说明共轴；区间、显著性、阈值须有数据依据。

默认保留原填充、渐变、透明层次、原色描边、标记和注释设计。不要顺手统一alpha、线宽、字体或重做框样式；不同视觉效果不是必要的数据替换。模板已有的短指标框、引线、白底标注不是禁用对象；模板没有的处理过程、筛选规则和图形阅读说明写入随图说明；不默认新增图内说明框。必要图内补充沿用模板设计，检查遮挡。训练/测试分界、可行域和汇总统计等是有条件的信息层：缺少真实定义时移除相应构件，不临时发明比例或分析结论来填空。数据不支持某层、用户指定变更或可读性需要时适配，在已有说明中简记原因；不按代码改动百分比、固定画布或注释数量验收。

## 3. 用事实核对

```text
python _utils/recipe_style_review.py --workspace . --summary --output .vivid/source-review.json
```

默认使用已保存底稿；工具比较新旧调用位置、显式参数、透明度/轮廓/渐变设置、画布/面板/字体以及注释框，并保存完整diff。`--candidate` 可只检查一张。兼容包内 `scripts/review_recipe_style.py --id <ID> --candidate <file>` 和显式 `--baseline`；未登记的旧脚本回退到卡片快照，不冒称证明过源码起点。

静态检查不运行候选脚本，不能推断任意动态值、辅助文件、分支实际执行或坐标含义。差异均为线索，合理的布局和统计适配可保留；不为消除提示改回演示数据。语法、底稿哈希、缺失文件等确定性问题报错；**无提示不等于视觉或数据通过**。

按 `original/review-policy.md` 对照实际图与原预览：关键图层是否可见，新增内容是否有依据，是否漏组/漏站点、混单位、遮挡或裁切；坐标刻度是否可读，指标分组与有效样本数是否对应真实评估范围。沿用现有检查与修复轮次，不另开自评循环。卡片快照解释主要示例，当前完整配方决定实际来源；全库一致性由维护测试保障。
