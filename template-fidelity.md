# 从完整源码适配，保留模板特点

用于选定模板后的绘制、修图和换色。与现有配色及审图指导一起使用，不重新选图或增加修复轮次。

## 动笔前

从当前完整配方中取出所选示例的完整代码，复制为本次绘图脚本，在这个底稿里适配输入、计算、变量绑定、标签和必要布局。保留原有绘制函数、图层及样式设置；不要看完模板后凭印象重新写一份“同类型图”。108 个配方用 `original/resources/assets/shared-scripts/get_recipe.py` 加载，完整组合沿用自己的模板说明。选择其他变体时使用该变体的完整源码。

读取所选卡片的“源码保真要点”，结合预览和实际代码，先确认本图的主要信息层和视觉关系，再替换数据。例如：折线色带的宽度与透明度对应、雨云的错位结构、浅填充与深轮廓、矩阵的色阶含义、三维图的投影。卡片给出保留重点、允许适配范围与源码行链接，只读所选模板即可。

卡片数值描述的是该示例的实现，不是全库通用阈值。换色仍遵循统一绘图指导与配色说明，尽量保留原来的浅深、透明叠加、轮廓和连续/离散映射关系；不是把所有图元重设成一种实色。统计区间、密度、轨迹或标记仍须有数据依据。数据不支持、用户明确要求或可读性确实需要改变时，做有依据的局部适配，保留其余结构，在已有任务记录中简述原因即可。

## 修改后核对

沿用现有实图审查：并看模板预览与当前输出，核对卡片列出的特点是否仍可见。图层仍在但太淡、被盖住或宽度不合适，也可能失去原来的效果。尺寸和坐标调整应按表达目的判断，不把一切形变都认定为错误。

改动较多或怀疑遗漏时，可用只读源码差异提示，帮助定位后再判断：

```bash
python "<SKILL>/scripts/review_recipe_style.py" --id basic.line --candidate "<WORKSPACE>/figures/gen_line.py" --output "<WORKSPACE>/style-review.json"
```

默认比较卡片快照；若本次使用其他变体或较新的配方，传入从该完整源码保存的底稿：

```bash
python "<SKILL>/scripts/review_recipe_style.py" --id basic.line --baseline "<WORKSPACE>/_utils/line-source.py" --candidate "<WORKSPACE>/figures/gen_line.py" --output "<WORKSPACE>/style-review.json"
```

提示区分样式值变化、重复次数变化、色图变化、数据映射变化和无法静态确认的表达式。相同样式的重复次数变化可能只是组数变化，也可能影响透明图层叠加，结合源码和实图判断；不要为了消除提示把合理适配改回示例。辅助函数或等价实现移到其他文件时，直接阅读其实现。

工具只解析 Python，不运行绘图代码；支持字面量和有界循环，不推断运行时分支、任意函数或跨文件依赖。它不全面核验布局、配色计算或最终可见性，也不输出视觉通过结论。**没有提示不等于保真通过。**提示不阻断绘图，不替代现有审图，也不另开修复循环。

卡片覆盖 109 个主要示例，不代表原章节所有独立变体。快照行号用于解释预览；当前完整配方始终是实际绘制依据。维护图库时核对快照与当前源码的一致性，不要求每次绘图重新验证全库。
