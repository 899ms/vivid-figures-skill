# 两种模式下的配色选择

鲜艳舒适型（expressive）与稳重科研型（restrained）均提供下面全部配色选项。模式决定原有绘图指导，配色只决定颜色搭配；选择同一配色不改变两种模式各自的选图、构图、模板、尺寸、渐变、透明度及审图修复要求。

## 使用前询问

在本次绘图开始前，若用户尚未明确选择配色，也未明确要求沿用现有配色、使用默认或由 Agent 决定，先列出以下选项并询问，收到选择后再绘制。模式也未确定时，将模式和配色合并成一次简短询问；只缺配色时只问配色。

- 鲜艳版：模式原配色（珊瑚青绿）、海洋暖橙、鸢尾杏桃、森林日光、浆果冰蓝，也可选科研原配色或自定义颜色。
- 稳重版：模式原配色（科研原配色）、珊瑚青绿、海洋暖橙、鸢尾杏桃、森林日光、浆果冰蓝，也可自定义颜色。

例如：“使用鲜艳版还是稳重版？配色选模式原配色、珊瑚青绿、海洋暖橙、鸢尾杏桃、森林日光、浆果冰蓝或科研原配色？也可以提供自定义颜色。”

同一任务已明确选定时，后续补图、修图和继续执行沿用该选择，不逐图重复询问。用户说“默认”就用对应模式原配色；说“你决定”时可直接选择并简短告知。未回复不视为同意默认。

## 配色表

以下颜色按顺序使用，四套新增配色与预览图一致。

| 选项 | 颜色顺序 / 原有配置 |
|---|---|
| 珊瑚青绿（鲜艳版原配色） | `#4ECDC4,#FF6B6B,#45B7D1,#F7A072,#A06CD5,#F79256,#7DCFB6` |
| 海洋暖橙 | `#3085C3,#F29155,#48BFCC,#E8BC59,#697CC5,#70C5B0,#D98096` |
| 鸢尾杏桃 | `#9975C7,#EFA582,#6D92CF,#D780A2,#B2A0DE,#76BFB5,#DFC47C` |
| 森林日光 | `#3BA77C,#E5B64E,#55BAC0,#ED8A54,#8FBC6A,#5999B7,#CA8CAE` |
| 浆果冰蓝 | `#CC5F91,#70BCE0,#9679CA,#EF9A94,#55B7AF,#D9B565,#7995CD` |
| 科研原配色（稳重版原配色） | 沿用 `MH_DATA_FIG_PALETTE=colorblind`，不改变原工具实现 |

## 使用现有接口

在任务工作区的 `CLAUDE.md` 中保留其他内容，更新对应的原有标记，不重复追加同名标记。选择上表五套具体彩色方案之一或用户提供的自定义颜色时，使用 `MH_DATA_FIG_PALETTE=custom`，将颜色顺序写入 `MH_DATA_FIG_COLORS`。例如，两种模式选择海洋暖橙时均使用：

```html
<!-- MH_DATA_FIG_PALETTE=custom -->
<!-- MH_DATA_FIG_COLORS=#3085C3,#F29155,#48BFCC,#E8BC59,#697CC5,#70C5B0,#D98096 -->
```

选择科研原配色时使用 `<!-- MH_DATA_FIG_PALETTE=colorblind -->`，移除失效的 `MH_DATA_FIG_COLORS` 标记。模式仍记录在原有任务计划中，不从色板反推模式；`MH_DATA_FIG_STYLE` 继续遵循原模式/用户已有版式设置，不因换色修改。

生成代码继续在 `setup_style()` 后读取 `pu.PALETTE` / `pu.COLORS`。本选择仅覆盖原模式文档及宿主适配中默认色板、默认具体色相的描述；其余提示词与代码不变。分类色、主色和强调色随选择变化，浅填充、原色描边和透明层次仍按原配方执行。连续量的渐变按原模板和数据语义处理，不因更换分类色板而统一改成主题单色或简化渐变。流程/框图统一主色、工程图保留原配色、批注白底等原有范围规定继续适用。
